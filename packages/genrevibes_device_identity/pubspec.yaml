name: genrevibes_device_identity
description: Provider-neutral stable install identity with optional advertising and vendor identifiers.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_storage: ^0.1.0-dev.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

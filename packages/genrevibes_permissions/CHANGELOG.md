# Changelog

## 0.1.0-dev.1

- Add permission kinds and states, `PlatformFacts`, `MediaPermissionPolicy`,
  `PermissionRequestThrottle`, `PermissionCoordinator`, the provider contract,
  and `PermissionRationaleView`.

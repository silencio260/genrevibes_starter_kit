name: genrevibes_app_links
description: Provider-neutral store, support, privacy and sharing links for GenRevibes apps.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  genrevibes_core: ^0.1.0-dev.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

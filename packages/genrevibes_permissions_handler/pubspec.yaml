name: genrevibes_permissions_handler
description: permission_handler adapter for GenRevibes runtime permissions.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  device_info_plus: ">=10.1.2 <12.0.0"
  flutter:
    sdk: flutter
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_permissions: ^0.1.0-dev.1
  permission_handler: ">=11.3.1 <12.0.0"

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  genrevibes_storage: ^0.1.0-dev.1

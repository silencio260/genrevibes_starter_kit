name: genrevibes_analytics_mixpanel_replay
description: Optional Mixpanel session-replay integration for GenreVibes applications.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  genrevibes_core: ^0.1.0-dev.1
  mixpanel_flutter_session_replay: ">=0.1.0-beta.3 <0.2.0"

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

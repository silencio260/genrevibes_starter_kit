name: genrevibes_ads_test
description: Shared contract-test harness for GenRevibes ad provider adapters.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  genrevibes_ads: ^0.1.0-dev.1
  genrevibes_core: ^0.1.0-dev.1
  test: ^1.25.0

dev_dependencies:
  lints: ^4.0.0

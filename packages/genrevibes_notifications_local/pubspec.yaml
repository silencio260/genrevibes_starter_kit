name: genrevibes_notifications_local
description: Persistent local notification scheduler adapter for the GenreVibes notifications contract.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ">=19.5.0 <20.0.0"
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_notifications: ^0.1.0-dev.1
  timezone: ">=0.10.0 <0.11.0"

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

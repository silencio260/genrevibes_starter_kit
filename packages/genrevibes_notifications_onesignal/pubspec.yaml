name: genrevibes_notifications_onesignal
description: OneSignal adapter for the provider-neutral GenreVibes notifications contract.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_notifications: ^0.1.0-dev.1
  onesignal_flutter: ">=5.4.1 <6.0.0"

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

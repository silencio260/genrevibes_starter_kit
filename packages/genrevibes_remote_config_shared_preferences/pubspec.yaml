name: genrevibes_remote_config_shared_preferences
description: SharedPreferences last-known-good cache for GenreVibes remote config.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_remote_config: ^0.1.0-dev.1
  shared_preferences: ">=2.2.0 <3.0.0"

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

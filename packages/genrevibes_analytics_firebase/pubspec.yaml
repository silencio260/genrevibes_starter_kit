name: genrevibes_analytics_firebase
description: Firebase Analytics sink adapter for the GenreVibes analytics pipeline.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  firebase_analytics: ">=11.4.2 <12.0.0"
  firebase_core: ">=3.11.0 <4.0.0"
  flutter:
    sdk: flutter
  genrevibes_analytics: ^0.1.0-dev.1
  genrevibes_core: ^0.1.0-dev.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  genrevibes_analytics_test: ^0.1.0-dev.1

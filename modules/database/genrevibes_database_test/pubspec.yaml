name: genrevibes_database_test
description: Shared contract-test harness for GenRevibes document store adapters.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_database: ^0.1.0-dev.1
  test: ^1.25.0

dev_dependencies:
  lints: ^4.0.0

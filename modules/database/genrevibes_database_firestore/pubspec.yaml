name: genrevibes_database_firestore
description: Cloud Firestore adapter for the GenRevibes document store contract.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  cloud_firestore: ">=5.6.0 <7.0.0"
  firebase_core: ">=3.11.0 <4.0.0"
  flutter:
    sdk: flutter
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_database: ^0.1.0-dev.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  genrevibes_database_test: ^0.1.0-dev.1

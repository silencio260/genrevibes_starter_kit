name: genrevibes_feedbacknest
description: FeedbackNest adapters for GenRevibes feedback submission and rating capture.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  feedbacknest_core: ">=0.0.8 <0.0.9"
  flutter:
    sdk: flutter
  genrevibes_app_rating: ^0.1.0-dev.1
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_feedback: ^0.1.0-dev.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

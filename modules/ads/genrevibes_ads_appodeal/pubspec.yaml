name: genrevibes_ads_appodeal
description: Appodeal mediation adapter for the GenRevibes ads contract.
version: 0.1.0-dev.3
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  genrevibes_ads: ^0.1.0-dev.4
  genrevibes_core: ^0.1.0-dev.1
  stack_appodeal_flutter: ">=4.2.0 <5.0.0"

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

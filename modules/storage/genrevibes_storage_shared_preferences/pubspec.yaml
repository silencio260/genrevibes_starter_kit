name: genrevibes_storage_shared_preferences
description: SharedPreferences implementation of the GenRevibes key-value storage contract.
version: 0.1.0-dev.2
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  genrevibes_core: ^0.1.0-dev.1
  genrevibes_storage: ^0.1.0-dev.1
  shared_preferences: ">=2.2.0 <3.0.0"

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

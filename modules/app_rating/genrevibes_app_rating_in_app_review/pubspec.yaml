name: genrevibes_app_rating_in_app_review
description: Platform in-app review adapter with a store-listing fallback for GenRevibes.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  genrevibes_app_rating: ^0.1.0-dev.1
  genrevibes_core: ^0.1.0-dev.1
  in_app_review: ">=2.0.9 <3.0.0"
  url_launcher: ">=6.2.0 <7.0.0"

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  genrevibes_app_rating_test: ^0.1.0-dev.1

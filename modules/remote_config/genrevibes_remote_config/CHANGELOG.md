# Changelog

## 0.1.0-dev.2

- Add `RemoteConfigStringListCodec`, a JSON array of strings where a blank
  value means an empty list.

## 0.1.0-dev.1

- Add typed keys, schema validation, safe snapshots, and last-known-good cache.
- Add a provider-neutral coordinator with failure-safe refresh behavior.

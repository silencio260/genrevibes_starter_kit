name: genrevibes_notifications
description: Provider-neutral push notification state, diagnostics, local scheduling contracts, and campaign coordination.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  genrevibes_core: ^0.1.0-dev.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
